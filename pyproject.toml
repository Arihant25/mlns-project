[project]
name = "mlns-project"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "torch>=2.0.0",
    "transformers==5.0.0rc3",
    "PyTDC",
    "scikit-learn",
    "scipy",
    "requests",
    "rdkit",
    "xgboost",
    "setuptools<79",
    "chembl-webresource-client",
]
